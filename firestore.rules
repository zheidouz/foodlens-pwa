rules_version = '2';

// Locked-down rules for Sprint 1 — will refine in Sprint 3
service cloud.firestore {
  match /databases/{database}/documents {
    // Deny all by default
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
